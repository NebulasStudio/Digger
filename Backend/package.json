{
  "name": "@sandsunder/nakama-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Authoritative Nakama runtime for Sandsunder account progression and match settlement.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.cjs",
    "check": "npm run test"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.4",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=22"
  }
}
