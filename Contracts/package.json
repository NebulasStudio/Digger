{
  "name": "@sandsunder/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Versioned service contracts for Sandsunder.",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "npm test"
  },
  "devDependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "yaml": "2.9.0"
  },
  "engines": {
    "node": ">=22"
  }
}
